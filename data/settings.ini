# settings.yaml
accounts:
  accounts_file: ""  # Path to accounts file
  proxies_file: ""   # Path to proxies file
  referral_codes: [] # List of referral codes

execution:
  threads: 5
  delay:
    min: 1
    max: 2

captcha:
  service: "capmonster"
  api_key: ""

# Optional: Add validation rules and descriptions
validation:
  delay:
    min_allowed: 1
    max_allowed: 30
  threads:
    max_allowed: 10
